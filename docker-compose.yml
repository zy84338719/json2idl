version: "3"
services:
  json2idl:
    image: zy84338719/json2idl:latest
    container_name: json2idl_server
    restart: always
    ports:
      - '59990:59990'
    networks:
      - base
networks:
  base:
    external: true
